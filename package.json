{
  "name": "docsearchengine",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix backend ci && npm --prefix backend run build && node -e \"fs=require('fs');p='backend/dist/public';fs.mkdirSync(p,{recursive:true});fs.cpSync('frontend/dist',p,{recursive:true});\"",
    "start": "npm --prefix backend start"
  }
}
