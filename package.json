{
  "name": "docsearchengine",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix frontend ci && npm --prefix backend run build && npm --prefix frontend run build && node ./scripts/move-ui.mjs",
    "start": "node backend/dist/server.js",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install"
  }
}
