{
  "name": "docsearchengine",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix backend ci && npm --prefix frontend run build && npm --prefix backend run build && node scripts/move-ui.mjs",
    "start": "node backend/dist/server.js"
  },
  "overrides": {}
}
